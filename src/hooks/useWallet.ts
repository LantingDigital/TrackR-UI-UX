/**
 * useWallet Hook
 *
 * Convenience hook for accessing wallet context.
 * Re-exports useWalletContext with a simpler name.
 */

export { useWalletContext as useWallet } from '../contexts/WalletContext';
