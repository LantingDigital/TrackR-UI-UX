/**
 * Screenshot Mode
 *
 * When true, stores populate with mock data instead of syncing from Firestore.
 * Use for taking App Store screenshots, social media content, portfolio images.
 *
 * Flip to `true`, hot reload, take screenshots, flip back to `false`.
 */
export const SCREENSHOT_MODE = false;
